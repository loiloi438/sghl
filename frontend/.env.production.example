# URL de l'API en production (si différente du même domaine)
# VITE_API_BASE_URL=https://api.example.com/api/v1

# Stripe (portail patient — paiement carte)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_replace_me
